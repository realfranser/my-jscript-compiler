
let boolean b;
let number k;
function number factorial(number x, boolean c)
/* se define la función recursiva con un parámetro, 
   que oculta a la variable global de igual nombre */
{
    if (x == 1) x++;

}	/* la función devuelve un entero*/
factorial(1, b);